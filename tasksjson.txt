Vewrsuche falls es wieder fehlschlägt mit return -1

{
    "type": "shell",
    "label": "C/C++: gcc.exe build active file",
    "command": "gcc",
    "args": [
        "-g",
        "${file}",
        "-o",
        "${fileDirname}\\${fileBasenameNoExtension}.exe"
    ],
    "options": {
        "cwd": "${fileDirname}"
    },
    "problemMatcher": [
        "$gcc"
    ],
    "group": {
        "kind": "build",
        "isDefault": false
    },
    "detail": "Manueller Shell-Build"
},